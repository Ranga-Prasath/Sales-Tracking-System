# Sales Tracking System

A simple Python application for tracking sales records with Azure DevOps CI/CD integration.

## Features
- Add new sales records
- View sales history
- User-friendly menu interface

## Testing
Run tests locally:
```bash
python -m pytest test_sales.py -v
```

## Azure DevOps Integration
Tests run automatically on every push to main branch via `azure-pipelines.yml`
